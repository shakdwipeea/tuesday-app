<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="contact"
            type="com.shakdwipeea.tuesday.data.entities.User" />

        <variable
            name="provider"
            type="com.shakdwipeea.tuesday.data.entities.Provider" />

        <variable
            name="actionHandler"
            type="com.shakdwipeea.tuesday.home.home.ContactItemActionHandler" />

        <variable
            name="itemViewModel"
            type="com.shakdwipeea.tuesday.home.notification.NotificationItemViewModel" />
    </data>

    <LinearLayout
        android:layout_margin="10dp"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(view) -> actionHandler.openProfile(view, contact)}">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_pic"
            android:layout_width="56dp"
            android:layout_height="56dp"
            />

        <ImageView
            android:id="@+id/placeholder_profile_pic"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:visibility="gone"/>

        <LinearLayout
            android:layout_marginLeft="16dp"
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/name"
                android:textSize="20sp"
                android:text="@{contact.name}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <TextView
                android:id="@+id/phone_number"
                android:textSize="20sp"
                android:text="@{provider.name}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </LinearLayout>

        <ImageButton
            android:src="@drawable/ic_check_black_24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> itemViewModel.handleApproval(true, provider, contact)}"
            />

        <ImageButton
            android:src="@drawable/ic_clear_black_24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> itemViewModel.handleApproval(false, provider, contact)}"
            />

    </LinearLayout>

</layout>